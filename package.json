{
  "name": "gestor-gastos",
  "version": "1.0.0",
  "description": "Sistema de gestão de gastos com Electron e SQLite",
  "main": "main.js",
  "author": "Edson",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w better-sqlite3",
    "icons": "node scripts/generate-icons.js",
    "pack": "npm run icons && electron-builder --dir",
    "dist": "npm run icons && set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --win --x64",
    "dist-installer": "npm run icons && set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --win --x64",
    "package-exe": "npm run icons && electron-packager . \"Gestor de Gastos\" --platform=win32 --arch=x64 --out=dist --overwrite --ignore=\"node_modules/(electron-builder|@electron)\" --icon=build/icon.ico",
    "postinstall": "electron-builder install-app-deps"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "chart.js": "^4.5.1"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.2",
    "electron": "^31.0.0",
    "electron-builder": "^24.9.1",
    "electron-packager": "^17.1.2",
    "png-to-ico": "^2.1.8"
  },
  "build": {
    "appId": "com.edson.gestorgastos",
    "productName": "Gestor de Gastos",
    "directories": {
      "buildResources": "build"
    },
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowElevation": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Gestor de Gastos"
    }
  }
}
